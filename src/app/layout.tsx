// Next.js App Router entry — re-exports the real layout that lives in
// the `interfaces/` layer, preserving the Clean Architecture boundary.
export { default, metadata } from '../interfaces/web/app/layout';
